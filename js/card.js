class Card {
    constructor(Id, front_head, front_body, back_head, back_body, box, boxseq){
        this.Id = Id,
        this.Front_Head = front_head,
        this.Front_Body = front_body,
        this.Back_Head = back_head,
        this.Back_Body = back_body,
        this.Box = box,
        this.BoxSeq = boxseq
    }
}